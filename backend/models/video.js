class Video {
    constructor(id, heading, date,imgUrl,videoUrl) {
            this.id = id;
            this.heading = heading;
            this.date = date;
            this.imgUrl = imgUrl;
            this.videoUrl = videoUrl;
    }
}

module.exports = Video;